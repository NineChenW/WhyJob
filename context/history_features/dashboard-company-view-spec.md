# Dashboard Company View Spec

## Overview

Replace the dummy data displayed in the “@src/app/companies/[id]/page.tsx”, with actual data from the database. It should be from our Neon database using Prisma.

## Requirements

- Create src/lib/db/content.ts with data fetching functions
- Fetch company's content data directly in server component
- Implement the related content parser and component
